export const backendAgent = {
  id: "backend",
  responsibility: "Backend services, APIs, validation, persistence, reliability, and error handling.",
  prompt: "Act as the backend specialist. Prefer typed contracts, clean service boundaries, secure defaults, and testable code."
} as const;
