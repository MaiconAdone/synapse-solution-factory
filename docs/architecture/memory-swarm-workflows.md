# Memory, Swarm, and Workflows

## Memory

Hybrid memory combines:

- working memory for active operations
- episodic memory for workflow traces and decisions
- semantic memory for reusable knowledge and retrieval

## Swarm

The swarm is configured as hierarchical-mesh with 15 maximum agents and
distributed coordination.

## Workflows

Base workflows:

- `new-ai-project`
- `rag-build`
- `ml-release`
